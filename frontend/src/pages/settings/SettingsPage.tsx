import { PagePlaceholder } from "@/components/shared/PagePlaceholder"

export default function SettingsPage() {
  return (
    <PagePlaceholder
      title="Pengaturan"
      description="Master data: cabang, dokter, tindakan & tarif, odontogram, pengguna."
    />
  )
}
